#include "builtIns.h"
#include <stdlib.h>

/**
 * Methods for all built in shell commands
 */

void exitShell(void) {
    exit(0);
}

int cd(char* args[]) {

    return -1;
}

int path(char* args[]) {

    return -1;
}